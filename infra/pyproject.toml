[project]
name = "ogx-infra"
version = "0.1.0"
description = "OGX local development setup and verification"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "openai>=1.0",
    "httpx>=0.27",
]
