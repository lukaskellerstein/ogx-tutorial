[project]
name = "ogx-tutorial-L2-M2-vllm-integration"
version = "0.1.0"
description = "L2-M2.2 — OGX + vLLM Integration on OpenShift"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27",
]
